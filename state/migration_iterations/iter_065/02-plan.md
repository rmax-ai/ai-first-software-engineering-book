# Plan

1. Verify the previous guidance and target file.
2. Update exactly one line in `state/migration_iterations/iter_024/07-summary.md`.
3. Capture before/after evidence with `rg` and `git show`.
4. Write iteration artifacts with results and a single next task.
