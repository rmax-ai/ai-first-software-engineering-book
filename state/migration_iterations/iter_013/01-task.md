# Task

## Selected task title
Run kernel-level mock regression on the eligible chapter and capture deterministic evidence.

## Why this task now
`iter_012` identified this as the top remaining migration validation item. The chapter governance state now has one eligible chapter (`01-paradigm-shift`).

## Acceptance criteria for this iteration
- Execute kernel regression on an eligible chapter.
- Verify planner/writer/critic path can run in a migration-relevant mode.
- If blocked, record exact failure and the smallest unblock step.
