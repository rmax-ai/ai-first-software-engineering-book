# Task

## Selected task title
Normalize one remaining bare `python` smoke-test snippet to `uv run python`.

## Why this task now
`iter_064/06-next-iteration.md` identified `state/migration_iterations/iter_024/07-summary.md` as the next direct legacy snippet.

## Acceptance criteria for this iteration
- Confirm the legacy snippet existed in `state/migration_iterations/iter_024/07-summary.md` before edit.
- Update the snippet to `uv run python state/copilot_sdk_smoke_test.py --mode fallback-timeout`.
- Capture `rg` and `git` evidence for the edit.
