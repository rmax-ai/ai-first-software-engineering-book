# Plan

1. Update `state/copilot_sdk_smoke_test.py` to add a `bootstrap-failure` mode and CLI wiring.
2. Implement deterministic failure simulation and assert the bootstrap error text.
3. Run smoke tests for `stub`, `sdk-unavailable`, and `bootstrap-failure`.

## Files expected to change
- `state/copilot_sdk_smoke_test.py`
